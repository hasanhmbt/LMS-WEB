@model VwAuthor



<h1>@Model.Name @Model.Surname</h1>

<div class="row">
    <div class="col-md-3">
        <img src="@Model.ImagePath" asp-append-version="true" width="290" height="350" />
    </div>
    <div class="col-md-9">
        <p>@Model.Description</p>
        <h2>number of books:@Model.Books</h2>
        @*<ul>
            @foreach (var book in Model.Books)
            {
                <li>@book.</li>
            }
        </ul>*@
    </div>
</div>